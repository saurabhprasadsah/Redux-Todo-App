const initialstate = {
   todos: ['breakfast', 'Lunch', 'dinner']
}


export const TodoReducer = (state = initialstate, action) => {
   switch (key) {
      case value:
         

         break;
      default:
         return state;   
   }

}